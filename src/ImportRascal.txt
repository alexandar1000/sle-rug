import Syntax;
import AST;
import CST2AST;
import Resolve;
import Check;
import Eval;
import ParseTree;
import Compile;



pt = parse(#Form, |project://QL/examples/binary.myql|);
ast = cst2ast(pt);
res = resolve(ast);
ch = check(ast, collect(ast), res);
//Input inpt = input("x_1_10", vbool(true));
ev = eval(ast, input("x_1_10", vbool(true)), initialEnv(ast));
